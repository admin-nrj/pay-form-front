import React from 'react';
import s from './card.module.css'

const Card = () => {
    return (
        <div className={s.cardWrapper}>
            <div className={s.card}>
                test
            </div>
        </div>
    );
};

export default Card;